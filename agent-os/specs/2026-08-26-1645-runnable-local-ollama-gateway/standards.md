# Standards for Runnable Local Ollama Gateway

No `agent-os/standards/index.yml` exists. The implementation follows current
repository conventions: Node.js built-ins, explicit consent for live execution,
bounded inputs and timeouts, no prompt logging, and public/synthetic data only.
